-- Differential smoke: DELIBERATE disagreement, used with --expect-disagree to
-- verify the runner's detection path stays alive. Sys.time() differs on every
-- run, so the observable outputs can never agree. (Also exercises a tracked
-- backend gap: native has no Sys.time — bugs/BUG-2.)

Sys.time ()
